{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "escritorio",
  "description": "La página la sirve el backend en 127.0.0.1, así que para Tauri es remota: sin esto no podría llamar a los comandos de main.rs (guardar con el diálogo nativo y recoger los archivos de «Abrir con…»). Sólo ese origen; cualquier otro no ve la IPC.",
  "windows": ["*"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": ["core:default"]
}
